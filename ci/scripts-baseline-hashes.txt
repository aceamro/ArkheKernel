# CI Script Integrity Hashes — scripts/*.sh
#
# Script integrity mechanism. The CI `lint` job runs `shasum -a 256 -c`
# against this baseline BEFORE invoking each script, ensuring no silent
# script tampering between baseline freeze and CI execution. Hash
# mismatch = CI fail (operator updates baseline + auditor approval PR).
# Header comments are stripped before `shasum -c` verification (same
# pattern as `verify-l0-baseline.sh`).
# Format: `<sha256>  <path>` — verbatim from `shasum -a 256` output.
#
# Update procedure: on intentional script change, regenerate baseline + auditor approval PR.
# Update command: shasum -a 256 scripts/verify-axiom-cite.sh
#          (replace this file's hash entry with the output line, preserve header block).
#
8088a828c9ce5326ed069c76a9502ded1cebcbbc125f0a9313147da2da1e6d94  scripts/verify-axiom-cite.sh
416d1198de6625ade57d1d398e48daab702718807b23277c207295c45bf43e2f  scripts/verify-l0-baseline.sh
