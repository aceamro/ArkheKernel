# CI Script Integrity Hashes — scripts/*.sh
#
# Reference: DIP-N6 M5.4 — script integrity mechanism. The CI `lint` job
# runs `shasum -a 256 -c` against this baseline BEFORE invoking each
# script, ensuring no silent script tampering between baseline freeze and
# CI execution. Hash mismatch = CI fail forced (operator updates baseline
# + auditor approval PR). verify-l0-baseline pattern carry — strip header
# comments before `shasum -c` verification.
# Format: `<sha256>  <path>` — verbatim from `shasum -a 256` output.
#
# Update procedure: on intentional script change, regenerate baseline + auditor approval PR.
# Update command: shasum -a 256 scripts/verify-axiom-cite.sh
#          (replace this file's hash entry with the output line, preserve header block).
#
dcc11d8dc67846c52e69c3194645d5f1d60e61c26ed2d4e9df0999c5ea924ea6  scripts/verify-axiom-cite.sh
416d1198de6625ade57d1d398e48daab702718807b23277c207295c45bf43e2f  scripts/verify-l0-baseline.sh
