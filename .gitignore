target/
node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# mdBook build output (regenerated by `mdbook build`)
book/book/
book/book-*/
runtime-book/book/
runtime-book/book-*/

# Repo 경계 외 임시 산출물 (BBS/Casino 등 별도 repo 이동 대상 보관소).
/tmp/

# Local tooling (editor/agent scratch state).
.claude/

# Apalache typecheck output (per-run timestamped directory).
_apalache-out/

# Internal team workflow doc — not for public publish.
developteamset.md
